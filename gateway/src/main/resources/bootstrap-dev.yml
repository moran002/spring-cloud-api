spring:
  cloud:
    nacos:
      server-addr: localhost:8848
      username: nacos
      password: xiyang
      # 配置的动态变更
      config:
        group: DEFAULT_GROUP
        namespace: dev
        file-extension: yml